extends layout

block content
    h1= title
    ul
        each genre in genrelist
            li
                a(href= genre.url) #{genre.name}
        else 
            li There are no genres copies in this library.