extends layout

block content
    h1= title
    ul
        each author in authorlist
            li 
                a(href= author.url) #{author.name}
                |  (#{author.date_of_birth_formatted} - #{author.date_of_death_formatted})
        else
            li There are no authors copies in this library.